import React from "react";
import styles from "./Home.module.css";

const Home = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>Welcome on the page of our service !</h1>
  </div>
);

export default Home;
